A tiny backend for Slack's webhook to create entities in Targetprocess

Typical usage:

`/new-bug Something is broken`

Expects some integration options, like Targetprocess installation URL, API token, etc.